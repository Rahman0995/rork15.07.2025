// Minimal mock data file - all mock data has been removed
// This file provides empty implementations to prevent import errors

export const mockUsers: any[] = [];
export const mockChats: any[] = [];
export const mockChatMessages: any[] = [];
export const mockReports: any[] = [];
export const mockTasks: any[] = [];

export const getUser = (id: string) => {
  // Return null since we're not using mock data anymore
  return null;
};

export const getUserById = (id: string) => {
  // Return null since we're not using mock data anymore
  return null;
};